const examples = {
htmleditor: `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Live HTML Editor</title><style>body{margin:0;font-family:Arial,sans-serif;display:flex;height:100vh;overflow:hidden;background-color:white;}textarea{width:50%;height:100%;border:none;padding:10px;font-family:monospace;font-size:16px;resize:none;outline:none;box-sizing:border-box;}iframe{width:50%;height:100%;border:none;box-sizing:border-box;}.editor-container{display:flex;width:100%;height:100%;}</style></head><body><div class="editor-container"><textarea id="htmlEditor" placeholder="Type your HTML here..."><h1>Hello World!</h1><p>This is a live HTML editor.</p></textarea><iframe id="preview"></iframe></div><script>const editor=document.getElementById('htmlEditor');const preview=document.getElementById('preview');function updatePreview(){preview.srcdoc=editor.value;}editor.addEventListener('input',updatePreview);updatePreview();</script></body></html>`,
orange: `<!DOCTYPE html><html><head><link rel="icon" type="image/x-icon" href="https://i.postimg.cc/FzMPQ7f4/Design-uten-navn-11-Photoroom-2.png"><link rel="stylesheet" href="style.css"><meta charset="utf-8"/><title>orange unlock --- site: N/A(for sharing)</title><style>body{text-align:center;background-color:orange;}h2{background-color:white;color:orangered;}p{background-color:white;color:orangered;}iframe{background-color:white;}button{color:orangered;background-color:white;border:none;border-radius:30px;padding:30px 40px;font-size:20px;cursor:pointer;}a{color:rgb(161,105,0);}input{background-color:white;color:orangered;border-radius:30px;height:40px;text-align:center}::placeholder{color:rgba(255,68,0,0.664);text-align:center;}</style></head><body><h2>orange unlock for sharing <br><img src="https://i.postimg.cc/vZhqM0dZ/Design-uten-navn-9.png" alt=""></h2><p>activate proxy <a href="https://cors-anywhere.herokuapp.com/corsdemo" target="_blank">here</a></p><input id="website" placeholder="enter/paste website url here" type="url"><br><br><button onclick="loadIframe()">Load page</button><button id="fsBtn">⇱︎fullscreen</button><br><br><iframe id="myFrame" width="1200" height="630" sandbox="allow-same-origin allow-scripts"></iframe><p>NOTE:<br>- this is a version for orange unlock that isnt numbered and contains css, js and html in one file(great for sharing, republishing and modifying.)<br>check out the main page for orange unlock <a href="https://hi0800.wixsite.com/orange-unlock">here</a></p><br><br><h2>how it works:</h2><p> Imagine you have a little webpage where you can type in a website's address, and with the click of a button, you get to see that website right there inside your page without opening a new tab. That’s basically what this site does.<br><br>Under the hood, when you type a web address and press the "Load page" button, the site tries to go fetch the content of that other website. But here’s the tricky part: modern browsers don’t like it when one site tries to grab stuff directly from another site because of security rules called CORS. To get around that, this site uses a middleman — a CORS proxy server — which fetches the website's content for you and hands it back<br><br>Then, instead of just linking directly to that website, it takes the raw HTML it got, wraps it up into something called a Blob (think of it like a little package of the website code), and then tells the iframe—the box on the page where the other site appears—to load that package.<br><br>This means you see the other website inside the box on the page you're on, almost like looking through a window. But because it’s still a bit tricky—some sites don’t play nicely, or scripts might not run perfectly. So, in short,<br><br>this site is like a helper that grabs and shows you other websites inside itself, cleverly dodging browser security limits by using a proxy and wrapping the content before showing it.</p><h2>what it's for:</h2><p>this site is basically a web unlocker that bypasses extentions like blocksi and other site restriction appliccations.</p><script>async function loadIframe(){try{const proxyUrl="https://cors-anywhere.herokuapp.com/";const targetUrl=document.getElementById("website").value;const response=await fetch(proxyUrl+targetUrl);if(!response.ok){throw new Error("Network response was not ok");}const text=await response.text();const blob=new Blob([text],{type:"text/html"});const blobUrl=URL.createObjectURL(blob);document.getElementById("myFrame").src=blobUrl;}catch(error){alert("Failed to load content: "+error.message);}}const iframe=document.getElementById("myFrame");const fsBtn=document.getElementById("fsBtn");const exitBtn=document.getElementById("exitBtn");fsBtn.addEventListener("click",()=>{if(iframe.requestFullscreen){iframe.requestFullscreen();}});exitBtn.addEventListener("click",()=>{if(document.exitFullscreen){document.exitFullscreen();}});document.addEventListener("fullscreenchange",()=>{if(document.fullscreenElement){fsBtn.style.display="none";exitBtn.style.display="inline-block";}else{fsBtn.style.display="inline-block";exitBtn.style.display="none";}});</script></body></html>`
};

const example = window.location.search.slice(1);

if (examples[example]) {
document.getElementById("code").value = examples[example];
document.getElementById("preview").srcdoc = examples[example];
}
